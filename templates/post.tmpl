{{define "title"}}{{ .Title }} | Seth{{end}}
{{define "content"}}
<h1 class="text-5xl font-bold">{{.Title}}</h1>
<div class="mt-2">
  <p class="text-gray-500">Author: {{.Author}}</p>
</div>
<div class="mt-4 prose max-w-full">{{.Content}}</div>
{{end}}
{{ template "core" .}}
